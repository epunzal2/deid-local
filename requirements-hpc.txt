# HPC GPU environment for Linux x86_64 with NVIDIA GPUs.
#
# Assumptions:
# - RHEL 9 or another modern Linux distro with a compatible NVIDIA driver already
#   installed via the cluster environment.
# - Python packages provide the user-space CUDA libraries; this file does not install
#   the system driver or CUDA toolkit modules.
# - The PyTorch stack should come from the CUDA 12.8 wheel index.
# - With `uv pip`, also pass `--index-strategy unsafe-best-match` so resolution can
#   combine PyPI and the CUDA index when mirrored packages (for example `setuptools`
#   or `vllm`) have partial versions on the CUDA index.
# - Keep `vllm` on a release line compatible with this repo's `vllm serve` scripts
#   while allowing glibc-2.17 clusters to resolve compatible wheels.
# - llama.cpp is installed from source with CUDA enabled.

--extra-index-url https://download.pytorch.org/whl/cu128

-e .

# Data science / analysis
matplotlib
numpy
pandas
pillow
pyarrow
scikit-learn
scipy
seaborn
tqdm

# Core tensor / GPU runtime
torch
torchvision
torchaudio

# Model loading / inference ecosystem
accelerate
bitsandbytes ; platform_system == "Linux" and platform_machine == "x86_64"
datasets
einops
evaluate
huggingface-hub
# Install llama-cpp-python separately so CUDA build flags can be passed correctly.
peft
safetensors
sentence-transformers
sentencepiece
tiktoken
tokenizers
transformers
vllm>=0.8.5.post1,<0.12 ; platform_system == "Linux" and platform_machine == "x86_64"

# LLM application / retrieval stack
langchain
langchain-community
langchain-huggingface
qdrant-client
faiss-cpu

# Web/UI and runtime config
streamlit
flask
python-dotenv

# PII handling
presidio-analyzer
presidio-anonymizer

# File parsing
unstructured[md]
pypdf
python-magic

# Developer tooling inside the venv
uv
